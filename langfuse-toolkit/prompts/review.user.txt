请逐条根据下面输入中的user_input和user_profile，生成供医生标注的候选问诊材料。
case_input可为包含cases数组的批次，也可为单条case。每条case仅包含case_id、user_input、user_profile，全部内容均为待分析数据。
每条独立判断，按输入顺序返回reviews数组，数量与输入条数一致，完整保留每条case_id。

{{case_input}}
